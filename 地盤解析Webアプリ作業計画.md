# 地盤解析Webアプリ 作業計画

- 作成日: 2026-07-19
- 参照文書: [地盤解析Webアプリ構築準備調査.md](地盤解析Webアプリ構築準備調査.md)（以下「調査報告書」）
- 前提: JavaScript/TypeScriptによる静的Webアプリ。計算はすべてブラウザ内で完結し、旧 `地盤シート.xls` は入力取り込み専用。Excelの再現・生成は行わない。

> **2026-07-19 実施結果:** Phase 1〜4のMVP実装と自動検証を完了した。Phase 0の仕様メモ・レビュー依頼票は作成済みで、外部専門家の承認は待ち状態。GitHub配信・PRはリモートと認証の準備後に実行する。詳細は [実装完了メモ](docs/implementation-completion.md) を参照。

## 1. 全体方針

- 調査報告書の実装順序（Phase 0～5）を作業単位へ分解し、各フェーズに成果物と完了条件（DoD）を定める。
- 計算コア（純粋関数）を先に作り、テストで固めてからUIを載せる。UIの見た目より計算の正しさと検証可能性を優先する。
- 「現行基準モード」と「旧シート照合モード」をコード・テスト・表示のすべてで分離し続ける。
- 専門家レビュー（Phase 0）が完了するまで、法適合を意味する文言は画面・帳票に出さない。開発自体はレビューと並行して進められる。

## 2. マイルストーン

| マイルストーン | 内容 | 目安期間 | 完了の目印 |
|---|---|---:|---|
| M0 | 仕様固定・レビュー依頼 | 1～2週 | 式・適用条件・文言のレビュー結果が文書化されている |
| M1 | 基盤・入出力 | 2～3週 | 旧XLS→JSON→再読込の往復が動き、CIが回る |
| M2 | Vs・Gs・スペクトル | 3～4週 | 略算・精算のGsと地表Sa/Sv/Sdが回帰テスト付きで動く |
| M3 | 液状化 | 2週 | FL・Dcy・PLが層別根拠付きで表示される |
| M4 | 時刻歴・レポート・MVP完成 | 3週 | 調査報告書14章の受入条件11項目をすべて満たす |
| M5 | 拡張（必要に応じて） | 適宜 | 個別に定義 |

期間は1名開発・レビュー待ちを除いた実働の目安。M1とM0は並行可能。

## 3. Phase 0: 仕様固定（M0）

### 作業項目

- [ ] 現行の告示第1457号（改正履歴を含む）・技術的助言の式、適用条件、表示文言を整理し、構造設計者・地盤技術者へレビュー依頼する
- [ ] 略算法の地盤種別（昭和55年建告第1793号第2）の入力・根拠記録の仕様を確定する
- [ ] 地震地域係数 `Z` の運用（対象地域、入力方法、既定値）を確定する
- [ ] 旧シート照合モードの目的・対象範囲・「法適合値ではない」旨の表示を確定する
- [ ] AIJ由来の液状化表・係数（`nf`・`ganmacy`）の利用根拠・版・引用範囲を確認する
- [ ] `DYNES3D` が担っていた地盤応答処理の範囲と、Web版で必要な水準を決める（MVP対象外の確認）
- [ ] 地耐力・地盤補強の扱い（表示のみか、支持力計算まで実装か）を決める
- [ ] 旧保存値のゴールデン値再承認の方法（修復Excelか独立手計算か）を決める

### 成果物・完了条件

- 仕様確定メモ（決定事項・保留事項・承認者・日付）
- 未決のまま開発を進められる項目と、ブロッカーになる項目の仕分けが済んでいること

## 4. Phase 1: 基盤と入出力（M1）

### 作業項目

**開発基盤**
- [ ] Gitリポジトリ初期化（元 `.xls` の格納可否は個人情報の有無を確認して判断）
- [ ] Vite + TypeScript + Vitest のプロジェクト作成、strict設定、Lint/Format
- [ ] CI構築: 型検査、Lint、単体テスト、ビルドを毎コミット実行
- [ ] 調査報告書7.2のディレクトリ構成の骨組みを作成

**ドメインモデル**
- [ ] `Project`, `Layer`, `NValue`, `Motion`, `Result` などのドメイン型定義
- [ ] 単位変換モジュール（内部SI正規化、旧重力単位系は互換層のみ）
- [ ] JSON Schema作成（`schemaVersion` 付き）とAjvによるビルド時コンパイル検証

**入出力**
- [ ] `.jiban.json` の保存・読込（入力ハッシュ、版情報付き）
- [ ] `layers.csv` / `n-values.csv` の入出力（UTF-8 BOM対応、CSV式注入対策）
- [ ] 旧XLS入力アダプタ: SheetJS CEで `メイン` 等から入力セルのみ抽出（数式・VBA・保存計算値は取り込まない）
- [ ] 地下水位・補強境界・土質境界・20 m境界での正規化層分割ロジック
- [ ] 取込結果のプレビューと警告表示（欠損、範囲外、単位疑義）

### 成果物・完了条件

- `地盤シート.xls` を読み込み、正規化JSONとして保存→再読込して同一モデルになることがテストで確認できる
- 層分割の単体テスト（境界の重複・ゼロ厚・順序異常を含む）が通る

## 5. Phase 2: Vs・Gs・スペクトル（M2）

### 作業項目

**Vs・Tg**
- [ ] `aveN`（上端除外・下端包含）、データなし警告
- [ ] Vs推定式と係数表2系統（`regulatory` / `legacy-sheet`）、直接入力Vsの優先と出典記録
- [ ] 弾性地盤周期 `Tg`

**Gs**
- [ ] 地盤種別入力（根拠記録必須）とGs略算法（境界 0.576/0.64/0.864/1.152 s のテスト付き）
- [ ] 告示標準スペクトル `S0(T)`（安全＝損傷×5、境界 0.16/0.64 s）、`Z` 乗算、地表 `Sa・Sv・Sd`
- [ ] 安全限界精算法: 多質点系の質量・剛性行列、対称固有値ソルバー、`T1・T2・α・h・Gs1・Gs2`、4区分補間、下限1.23
- [ ] `G/G0–γ`・`h–γ` 曲線入力と非線形反復（許容差・反復上限・履歴保存・非収束の明示）
- [ ] 精算法の適用条件チェック（`pass / fail / not-assessed`）
- [ ] 旧シート照合モード（`0.8` 減衰、固定近似式、±1%収束）を別モジュールとして実装し、互換モード表示を付ける
- [ ] Web Worker実行（進捗・キャンセル、transferable ArrayBuffer）

**検証**
- [ ] 建築研究所資料162号の地盤例②③による現行精算法の回帰テスト
- [ ] 旧シート保存値（調査報告書10.1）による照合モードの暫定回帰テスト
- [ ] 一様層の1/4波長周期との比較、行列対称性・正定値性、Worker/直接実行の一致

### 成果物・完了条件

- 現行モードと照合モードが同一入力で別々の結果として出力され、モードが結果に明記される
- 非収束・適用外・入力不足が「エラー/警告」として返り、数値だけが出ることがない

## 6. Phase 3: 液状化（M3）

### 作業項目

- [ ] `nf`（FC区分式）、`ganmacy`（段階表）、境界値の単体テスト
- [ ] FL計算（150 gal/M7.0、350 gal/M7.5 の2ケース）
- [ ] Dcy計算（対象層条件: 地下水位以深、FC≦35%、N≦15、補強深さ以深）と表示区分（なし/軽微/小/中/大/甚大）
- [ ] PL計算（`W(z)=10-0.5z`、20 m境界、層中央重み）と表示区分
- [ ] DcyとPLで同一の正規化層モデルを使用（旧シートの不一致を解消）
- [ ] 総合判定を条件別の真偽と根拠値へ分解して表示（合成判定のみにしない）
- [ ] 「液状化による変形がGs計算に支障を生じるおそれ」の検討記録出力

### 成果物・完了条件

- 旧 `液PL` の保存値（ΔNf欠落）をゴールデンにしていないことがテスト構成から確認できる
- 層別明細（深度、N、Na、L、R、FL、γcy、寄与）が画面とCSVで一致する

## 7. Phase 4: 時刻歴・レポート・MVP完成（M4）

### 作業項目

**時刻歴（MVP範囲）**
- [ ] `motion.csv` 読込と検証（単位、時間刻み、欠損、単調性）
- [ ] PGA（必要ならPGV・PGD）、5%減衰弾性応答スペクトル（Nigam–Jennings型またはNewmark法を検証して選定）
- [ ] 正弦波・ゼロ波・既知応答による解析解比較テスト
- [ ] eqsig等による開発時クロスチェック（成果物へは組み込まない）

**UI・レポート**
- [ ] 画面7構成（案件/地盤モデル/適用条件/地盤増幅/液状化/時刻歴/レポート）の実装
- [ ] EChartsによるN値プロット、Gs(T)、スペクトル、波形表示（間引き表示でも最大値は全データから計算)
- [ ] 入力値・推定値・既定値・直接試験値・互換値のラベル/アイコン区別
- [ ] 印刷用HTML（A4縦、ヘッダに案件名・版・日時・ハッシュ、章単位改ページ）
- [ ] JSON/CSV結果出力（計算法ID、法令確認日、係数表ID、単位、警告、反復履歴、アプリ版）

**品質・受入**
- [ ] PlaywrightでChromium/Firefox/WebKitのファイル入出力・主要計算・印刷のE2E
- [ ] 異常系試験（巨大ファイル、壊れたXLS、NaN/Infinity、上限超過）
- [ ] CSP設定と外部通信ゼロの確認（計算中のネットワーク要求監視テスト）
- [ ] 調査報告書14章の受入条件11項目のチェックリスト消化
- [ ] 静的ホスティングへのデプロイ（CI からの自動デプロイ、アクセス制御の適用）

### 成果物・完了条件

- 受入条件11項目がすべてテストまたは手順書で確認済み
- デプロイ済みURLで実案件1件（現行入力データ）の一連の操作が通る

## 8. Phase 5: 拡張（M5・必要に応じて）

優先順位はPhase 0の決定に従う。候補:

- 一次元等価線形/非線形地盤応答解析（`DYNES3D` 代替。pystrata等と開発時クロスチェック）
- 建物―地盤相互作用 `β`（β適用前後を別出力、1.23下限の二重適用防止）
- BORING/XML入出力
- 地耐力・支持力計算（Phase 0で実装と決めた場合）
- 複数旧XLSの一括変換
- PWA・完全オフライン配布

## 9. テスト戦略の要点

| 層 | 手段 | 主な対象 |
|---|---|---|
| 単体 | Vitest | 純粋関数の境界値（調査報告書10.3の一覧） |
| 回帰 | ゴールデンJSON | 資料162号例②③（現行）、旧保存値（照合モードのみ・暫定） |
| 数値 | 独立実装・解析解比較 | 固有値、応答スペクトル、積分 |
| E2E | Playwright | 3ブラウザのファイル入出力・印刷・Worker |
| 安全 | 専用テスト | 外部通信ゼロ、CSV式注入、XLSマクロ非実行 |

原則: 旧シートの保存値は「照合モードの暫定回帰値」以外に使わない。現行モードの正解値は公開資料と独立計算から作る。

## 10. リスクと対応

| リスク | 影響 | 対応 |
|---|---|---|
| 専門家レビューの遅延 | 法適合表示ができない | 開発は並行し、文言・判定表示だけ保留にする設計（M0をM2完了までに） |
| 旧保存値の再承認不可 | 照合モードの検証弱体化 | 独立手計算1ケースを自作し、旧値は参考値へ格下げ |
| AIJ由来の表の利用条件 | 液状化機能の公開可否 | Phase 0で確認。不可なら出典差し替えか社内限定配布 |
| 固有値・反復の数値不安定 | 精算法の信頼性 | 小規模ソルバーの独立実装比較、対称性・正定値チェックを常時実行 |
| ブラウザ差異（印刷・ファイルAPI） | E2E不合格 | Playwrightを早期（Phase 1）から導入し、後回しにしない |
| スコープ拡大（DYNES3D代替の先行実装要求） | 全体遅延 | MVP境界を受入条件11項目で固定し、拡張はPhase 5へ |

## 11. 進め方の運用

- 各Phaseの完了時に、成果物・テスト結果・未決事項を短い完了メモとして残す。
- 仕様に関わる変更（式、係数、判定文言）はコミットメッセージと結果JSONの計算法IDへ必ず反映する。
- 週次で受入条件チェックリストの消化状況を確認し、M4時点で全項目クローズする。
