# Third-party notices

This application bundles the following direct runtime dependencies. See each
linked project and the installed package metadata for the complete license
text and transitive dependency notices.

| Package | Purpose | License |
|---|---|---|
| React / React DOM | User interface | MIT |
| Vite | Static application build | MIT |
| SheetJS Community Edition | Legacy XLS input parsing | Apache-2.0 |
| Ajv | JSON Schema validation | MIT |
| Papa Parse | CSV input/output | MIT |
| Apache ECharts | Charts and print SVG | Apache-2.0 |
| Vitest | Unit tests | MIT |
| Playwright | Browser acceptance tests | Apache-2.0 |

No dependency is loaded from a CDN at runtime. Exact resolved versions and
integrity hashes are recorded in `package-lock.json`.
