# セキュリティ方針

- 計算はブラウザ内で完結し、入力ファイルを送信しない。
- 実行時CDN、外部API、テレメトリを使用しない。
- XLSのVBA、数式、外部リンク、埋込オブジェクトを実行しない。
- CSPで `object-src 'none'`、`connect-src 'self'` を設定する。
- JSON、CSV、XLS、時刻歴へサイズ・件数上限を設定する。
- CSVの文字列セルは先頭の `=`, `+`, `-`, `@` を無害化する。
- 計算結果には入力ハッシュ、アプリ版、計算法ID、警告を保存する。
